# 07. План Внедрения В Код (v2)

## 1. Принцип

Внедрение по фазам с измеримым критерием готовности.
Каждая фаза должна улучшать конкретные метрики, а не только визуальное впечатление.

**Новое в v2:** Добавлены фазы G-J для единого island scope и изостатики.

---

## 2. Фаза A: Геометрия плит 2.0 ✅ (done)

1. Перевести генерацию плит с «чистого Voronoi» на эволюционирующие полигоны.
2. Добавить неоднородные масштабы плит и микроплиты.
3. Добавить диффузные границы как отдельный тип.

Критерий готовности:
1. Снижение метрики «polygonality».
2. Уменьшение длин идеально прямых сегментов.

## 3. Фаза B: Кинематика на Эйлеровых полюсах ✅ (done)

1. Ввести `Ω` на плиту.
2. Считать `v_rel`, `v_n`, `v_t` на каждом сегменте.
3. Перевести типизацию границ на кинематику.

Критерий готовности:
1. Границы классифицируются стабильно и физически читаемо.
2. Нет «случайной» смены типа границы от шума.

## 4. Фаза C: Деформация и орогенез ✅ (done)

1. Ввести поля деформации `strain_*`.
2. Сделать variable-width орогенические пояса.
3. Убрать fixed-thickness uplift вдоль линии границы.

Критерий готовности:
1. Хребты не выглядят как равномерные полосы.
2. Появляются пояса и ветвления, похожие на реальные системы.

## 5. Фаза D: Океаническая кора и батиметрия 🔴 (next)

1. Ввести `ocean_age` и его адвекцию.
2. Реализовать GDH1 возраст-глубина (Stein & Stein, 1992):
   - `t <= 20 Myr: d = 2600 + 365 * sqrt(t)`
   - `t > 20 Myr: d = 5651 - 2473 * exp(-0.0278 * t)`
3. Добавить траншеи/хребты/трансформы/континентальные окраины.

Критерий готовности:
1. Исчезают одинаковые прибрежные каймы.
2. Глубины океана перестают быть «однотонными».

## 6. Фаза E: Осадки и релаксация

1. Ввести `sediment_thickness`.
2. Реализовать сглаживание и заполнение бассейнов осадками.
3. Добавить изостатическую релаксацию.

Критерий готовности:
1. Снижение искусственной резкости переходов.
2. Более естественные шельфы, склоны и равнины.

## 7. Фаза F: Калибровка и ранжирование

1. Встроить метрики из `06_DATASETS_AND_CALIBRATION.md`.
2. Автоматизировать массовые прогоны и score.
3. Фиксировать «golden seeds» для регрессий.

Критерий готовности:
1. Стабильный рост итогового score.
2. Снижение визуальных регрессий между коммитами.

---

## НОВЫЕ ФАЗЫ (v2)

## 8. Фаза G: Изостатика и физика коры

См. `12_ISOSTASY_AND_CRUSTAL_PHYSICS.md` для полных уравнений.

1. **Airy isostasy**: Заменить эмпирический relief calculation на `h = f(crust_thickness)`:
   - `h_land = (C - C_ref) * (rho_m - rho_c) / rho_c`
   - `d_ocean = (C_ref - C) * (rho_m - rho_c) / (rho_m - rho_w)`
   - Стандартные плотности: rho_c=2800, rho_m=3300, rho_w=1030, C_ref=35 km.
2. **Флексуральное сглаживание**: Заменить `buoyancy_smooth_passes` на Gaussian smooth с kernel = flexural wavelength:
   - `lambda = 2*pi * (D / (rho_m*g))^0.25`
   - `D = E * Te^3 / (12 * (1 - nu^2))`, E=100 GPa, nu=0.25.
   - Te из тектонической обстановки (5-120 km).
3. **NNR constraint**: В `compute_plates`, после генерации Omega, вычесть area-weighted mean.
4. **Slab-speed correlation**: Плиты с субдуцирующими slab'ами — быстрее (1.5-3.5×), без slab — медленнее (0.4×).

Критерий готовности:
1. Высота Тибет-подобных структур = 4000-6000 м при C = 70-85 км.
2. Океанические глубины = 4000-5500 м при C = 5-7 км.
3. Foreland basins появляются перед горными системами (прогиб от флексуры).

## 9. Фаза H: Абстракция сетки (GridConfig)

См. `11_UNIFIED_SCOPE_MODEL.md`.

1. Создать `GridConfig { width, height, is_spherical, km_per_cell_x, km_per_cell_y }`.
2. Рефакторнуть `compute_relief` принимать `GridConfig` вместо hardcoded WORLD_*.
3. Рефакторнуть noise-функции: `planar_fbm` как аналог `spherical_fbm` для flat grid.
4. Исправить hardcoded `ISLAND_WIDTH/HEIGHT` в `compute_climate_grid`, `carve_fluvial_valleys_grid`.
5. Удалить `generate_tasmania_height_map`, `apply_events_island`, `run_tasmania_scope`.

Критерий готовности:
1. `compute_relief(grid=planet)` даёт тот же результат, что и сейчас (регрессия).
2. `compute_relief(grid=island)` компилируется и запускается.
3. Нет упоминаний "tasmania" в коде.

## 10. Фаза I: Синтетический тектонический контекст для island scope

См. `13_ISLAND_SCALE_TERRAIN.md`.

1. Реализовать `generate_island_plate_context()`:
   - **Continental**: 2-3 плиты, NNW horst-graben, transform+divergent boundaries.
   - **Arc**: 2 плиты, субдукция, криволинейная дуга.
   - **Hotspot**: 1 плита, синтетический mantle plume blob.
   - **Rift**: 2 плиты, дивергентная граница, asymmetric shoulder.
2. Реализовать `generate_island_uplift_field()` — пространственный паттерн U(x,y):
   - Continental: alternating horst/graben bands + asymmetry.
   - Arc: Gaussian arc + trench + back-arc.
   - Hotspot: central Gaussian blob.
   - Rift: exponential decay from fault.
3. Реализовать `generate_lithology_field()` — пространственная карта K(x,y):
   - Continental: resistant dolerite caps + weak sedimentary basins.
   - Arc: uniform andesite/basalt.
   - Hotspot: uniform basalt.
   - Rift: mix.

Критерий готовности:
1. Continental island имеет несколько отдельных горных массивов (не один "бычий глаз").
2. Горы расположены вдоль структурной grain (NNW-trending для continental).
3. Долины врезаются между горными блоками.
4. Береговая линия нерегулярная (D > 1.2).

## 11. Фаза J: Stream Power Erosion

См. `13_ISLAND_SCALE_TERRAIN.md`, раздел 4.

1. Реализовать Braun-Willett O(n) implicit solver:
   - D8 flow routing.
   - Topological sort.
   - Drainage area accumulation.
   - Implicit elevation update: `h_new = (h_old + U*dt + factor*h_receiver) / (1 + factor)`.
2. Добавить hillslope diffusion: `dh/dt += kappa * laplacian(h)`.
3. Добавить климатическую модуляцию: `K_eff = K_lithology * (precipitation / P_ref)^0.7`.
4. Реализовать аналитическое стационарное решение для "ultra" preset:
   - `S = (U / (K * A^m))^(1/n)`.
   - Одно-проходное вычисление от устья к истокам.
5. Заменить текущие `carve_fluvial_valleys` / `carve_fluvial_valleys_grid` на stream power.

Критерий готовности:
1. Hack's Law выполняется: `h ∈ [0.5, 0.7]` для сгенерированных бассейнов.
2. Hypsometric integral в диапазоне 0.3-0.5 для continental islands.
3. Видимые V-образные долины, врезающиеся в highlands.
4. Климатическая асимметрия: windward side более изрезан, чем leeward.

---

## 12. Порядок внедрения (приоритет)

| # | Фаза | Зависимости | Влияние |
|---|------|-------------|---------|
| 1 | **H** (GridConfig) | Нет | Инфраструктура для всего остального |
| 2 | **G** (Изостатика) | H | Корректные высоты из толщины коры |
| 3 | **I** (Island context) | H | Island scope с тектоникой |
| 4 | **J** (Stream power) | H, I | Реалистичная эрозия для island |
| 5 | **D** (Ocean age) | G | Реалистичная батиметрия |
| 6 | **E** (Sediments) | D, G | Заполнение бассейнов |
| 7 | **F** (Scoring) | All | Объективная оценка |

## 13. Риски

1. Слишком тяжелая геофизика для real-time.
2. Переобучение метрик на один набор референсов.
3. Потеря детерминизма при параллелизации.
4. **Stream power solver может быть медленным на 1M+ ячейках с 10k+ шагов.**
5. **Аналитическое решение не учитывает transient features (rivers carving through new uplift).**

## 14. Как снижать риски

1. Разделить режимы preview/final по числу шагов времени.
2. Держать golden seeds и regression suite.
3. Фиксировать seeds и версии параметров в каждом прогоне.
4. **Braun-Willett O(n) обеспечивает линейную сложность — 10k steps × 500k cells ≈ 5B ops, ~2-5 sec в WASM.**
5. **Аналитическое решение для ultra preset — мгновенный стационарный рельеф без итерирования.**
